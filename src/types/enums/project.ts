export enum EDataSource {
  CONFLUENCE = 'vsearch/confluence',
  DATA_LAKE = 'vsearch/datalake',
  RAG_FOURTY_TWO = 'rag42/default',
}

export enum EProjcetTitle {
  CREATE = 'Create Project',
  SETTING = 'Setting Project',
}

export enum EAllowedExtensions {
  PDF = 'pdf',
  PPTX = 'pptx',
  DOC = 'doc',
  DOCX = 'docx',
  HWP = 'hwp',
  XLS = 'xls',
  XLSX = 'xlsx',
  CSV = 'csv',
  PPT = 'ppt',
}
